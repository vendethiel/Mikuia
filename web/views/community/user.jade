extends layout

block content
	+userHeader(Channel)
	.container
		.row
			.col-md-8
				h2 Derp
			.col-md-4
				.panel.panel-default
					.panel-heading Levels
					.panel-body
						+levelOverview(Channel.experience)
					.panel-footer
						a(href='/user/' + Channel.name + '/levels') See more >>